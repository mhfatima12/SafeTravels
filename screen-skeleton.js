import React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';

export default function ScreenName() {
  return (
    <View>
      <Text>Use when making new screen</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  
});
